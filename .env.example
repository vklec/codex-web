# Copy to `.env` and edit.

# Server port
PORT=8788

# Required auth for the web UI (change before exposing to the internet)
LOGIN_USERNAME=admin
LOGIN_PASSWORD=change-me
SESSION_SECRET=change-me-to-a-long-random-string

# Required: folder containing the repos you want to show in the Workspace UI.
# The server only allows starting terminals inside this directory.
REPO_ROOT=/Users/yourname/Sites

